# ── Domain ─────────────────────────────────────────────────────────────────
# Your root domain. Subdomains auto-configured:
#   zeroonec.xyz          → frontend
#   api.zeroonec.xyz      → backend
#   traefik.zeroonec.xyz  → traefik dashboard
#   *.zeroonec.xyz → ZeroClaw agent containers
DOMAIN=zeroonec.xyz

# ── Traefik ─────────────────────────────────────────────────────────────────
# Email for Let's Encrypt notifications
ACME_EMAIL=your@email.com

# Basic auth for Traefik dashboard
# Generate: htpasswd -nb admin yourpassword
# Example below is admin:admin (CHANGE THIS)
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xyz$$hashedpassword

# ── Database (Neon PostgreSQL) ───────────────────────────────────────────────
# Copy from Neon dashboard → Connection string
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# ── Backend secrets ──────────────────────────────────────────────────────────
# Random 64-char hex string: openssl rand -hex 32
JWT_SECRET=change_me_jwt_secret_64chars

# Random 32-char hex string (AES-256): openssl rand -hex 16
ENCRYPTION_KEY=change_me_32char_hex

# Firebase Admin SDK (for Google OAuth)
# Get from Firebase Console → Project Settings → Service accounts → Generate new private key
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# ── ZeroClaw agent image ──────────────────────────────────────────────────────
# Build with: docker build -f Dockerfile.zeroclaw -t zeroone/zeroclaw:latest ./zeroclaw
# Default uses upstream image; override with your custom build:
ZEROCLAW_IMAGE=ghcr.io/louisdevzz/zeroclaw:latest

# ── Frontend → deploy on Vercel (not on this server) ─────────────────────────
# Set these in Vercel Dashboard → Project → Settings → Environment Variables:
#
#   NEXT_PUBLIC_API_URL             = https://api.yourdomain.com
#   NEXT_PUBLIC_FIREBASE_API_KEY    = ...
#   NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN= ...
#   NEXT_PUBLIC_FIREBASE_PROJECT_ID = ...
#   NEXT_PUBLIC_FIREBASE_APP_ID     = ...

# ── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGIN=https://zeroonec.xyz,https://www.zeroonec.xyz
