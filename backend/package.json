{
  "name": "zeroone-backend",
  "version": "0.1.0",
  "description": "ZeroOne Orchestrator — ZeroClaw-as-a-Service backend",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "test:agent": "tsx scripts/test-agent.ts",
    "test:agent:skip-pull": "tsx scripts/test-agent.ts --skip-pull",
    "test:stripe": "tsx scripts/stripe-test.ts",
    "stripe:webhook": "stripe listen --forward-to localhost:3001/api/payments/webhook",
    "stripe:webhook:windows": "stripe listen --forward-to http://localhost:3001/api/payments/webhook",
    "stripe:trigger": "stripe trigger checkout.session.completed",
    "stripe:login": "stripe login"
  },
  "dependencies": {
    "@clerk/backend": "^2.32.1",
    "cors": "^2.8.5",
    "dockerode": "^4.0.2",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.38.0",
    "express": "^4.21.0",
    "postgres": "^3.4.5",
    "resend": "^6.9.2",
    "stripe": "^17.5.0",
    "svix": "^1.86.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/dockerode": "^3.3.32",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
